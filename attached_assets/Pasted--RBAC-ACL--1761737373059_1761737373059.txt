Безопасность и доступы

Тонкая модель прав (RBAC + объектные ACL): доступ к ребёнку/камере/чату только причастным родителям и стаффу данной группы; временные доступы для «доверенных лиц».

Чувствительные данные: отдельные политики для разделов «Здоровье» и «Документы» (строго ограниченный доступ и аудит действий).

Сессии и устройства: список активных сессий, принудительный разлогин.

Данные ребёнка

Структурированное «Здоровье»: аллергии (степень + протокол), питание (запреты), лекарства (дозировка/время/кто разрешил), поведенческие особенности, экстренные контакты.

Документы: мед-справка/прививки/согласия (файл + статус + напоминания), версии.

Видео и приватность

Мат-приватность медиа: водяной знак на экспортах, предупреждения при шаринге, ограничение скачивания, (на будущее: блюр лиц других детей).

Границы доступа к камерам: родитель видит только камеры своей группы; лимит одновременных сессий.

Коммуникации и уведомления

Тихие часы и уровни приоритета: «срочно», «обычно», «сводка»; утренняя сводка одним пушем.

Модерация вложений/форматов: лимиты размера, запрещённые типы.

Платежи

UX-поток оплаты: максимум 2 клика; рекламы нет на вкладке «Счета», рекомендации — только во вкладке «Услуги» или после оплаты.

Политики просрочки/напоминаний, экспорт квитанций (PDF), частичная оплата (если нужна).

Календарь/Дэшборд

Мини-календарь в дэшборде: без отдельного раздела; события открываются в боковой панели.

Ограничение контента на главной: до 4 карточек.

Локализация и форматирование

Полная RU/KZ локализация текстов, KZT формат валюты, таймзона Asia/Almaty везде (включая логи, графики, напоминания).

Доступность и качество

A11y: контраст 4.5:1, фокус-кольца, aria-лейблы для плеера/чатов/таблиц.

Error/Empty/Error-recovery: пустые состояния, сетевые ошибки, повтор отправки, skeleton-загрузки.

Полный набор возможностей для Replit + Требования к UI
1) Аутентификация и роли

Возможности

Replit Auth + роли: Parent, Teacher, Admin, Network Owner.

Переключение роли только при наличии прав.

UI требования

Экран входа → сразу Дэшборд своей роли.

Если у пользователя несколько ролей — выбор при входе (простое модальное).

Шапка: аватар + роль, меню профиля (выход, язык).

2) Организации и сущности (сад, группы, дети, стафф)

Возможности

CRUD: филиалы/группы/сотрудники/дети (RU/KZ).

Привязка детей и стаффа к группам.

UI требования

Разделы меню: Дети, Стафф.

Дети /children: таблица/список, поиск+фильтры (по группе/статусу), бейджи (⚠️ аллергия, 💊 лекарство, 📄 справка).

Профиль ребёнка (вкладки): День, Здоровье, Документы, Оплата.

Стафф /staff: список (ФИО, роль, группа, статус смены); профиль сотрудника.

3) Видео (mock)

Возможности

HLS-плеер, фейковые m3u8-потоки; таймлайн с маркерами событий.

Переключатель «низкая задержка/стабильный» (симулируемый).

UI требования

Кнопка «Смотреть» в профиле ребёнка → плеер в модалке/вкладке.

Маркеры на шкале времени (сон/еда).

Сообщение-заглушка, если доступа нет.

4) Чат (WebSockets)

Возможности

1:1 родитель-воспитатель, групповые каналы, вложения (фото/PDF), read receipts.

UI требования

/chat: слева список диалогов, центр — лента, внизу Composer (вложение/отправка).

Закреплённые диалоги сверху; индикатор «печатает».

Блок «важное» (шапка чата): бейджи ребёнка (аллергии/лекарства) — только чтение.

Тумблер «тихие часы» справа в шапке.

5) Посещаемость и «День ребёнка»

Возможности

Check-in/out с меткой времени; расписание дня; журнал (сон, еда, активность, настроение).

UI требования

День (в профиле ребёнка): карточки блоками в хронологии; «принято лекарство — 13:05».

На списке детей — быстрые действия (отметить приход/уход) для стаффа.

6) Платежи, подписки, услуги (Stripe)

Возможности

Счета/статусы, оплата, подписки, напоминания; каталог доп-услуг.

UI требования

/billing с вкладками: Счета | Подписки | Услуги.

Счета: кнопка «Оплатить» (≤2 клика), статус, детали + квитанция.

Подписки: тумблер автосписания, дата следующего списания.

Услуги: карточки (фильтры: возраст/дни/стоимость), запись → оплата.

Нет рекламы на «Счета»; рекомендации — только на «Услуги» и/или после оплаты внизу деталей счёта.

7) Уведомления (Push)

Возможности

Платежи due, дневные сводки, срочные объявления.

UI требования

Иконка «колокольчик»: табы «Платежи/Сообщения/События», отметка «прочитано».

Настройка Тихих часов в «Настройках».

8) Дэшборд (минимум)

Возможности

Карточка «Ваш ребёнок сегодня», «Платежи», «Сообщения», мини-календарь.

UI требования

Не более 4 карточек + мини-календарь, без скролла на 1366×768.

Кнопки «Подробнее/Перейти» ведут прямо в нужную вкладку/раздел.

Тумблер «Скрыть фото/видео».

9) Документы

Возможности

Хранение мед-справки/прививок/согласий; статусы и напоминания.

UI требования

Профиль ребёнка → Документы: список файлов (тип/дата/срок/статус); загрузка; бейдж на списке детей, если срок истекает.

10) Настройки

Возможности

Язык RU/KZ, валюта KZT, таймзона Asia/Almaty по умолчанию; доверенные лица; роли.

UI требования

Переключатель языков в шапке; предпросмотр формата дат/валюты.

Экран доверенных лиц (ФИО, связь, телефон).

Универсальные UI-требования (чтобы «реплит» ощущался продуманным)
A. Навигация

Левый выдвижной Sidebar (десктоп всегда виден; на мобиле — бургер), разделы:
Дэшборд / Дети / Стафф / Платежи / Чаты / Настройки.

Без отдельной кнопки «Календарь» (есть мини-виджет в дэшборде).

B. Консистентные паттерны

Вкладки внутри сущностей (у ребёнка: День / Здоровье / Документы / Оплата).

Боковые панели (drawer) для деталей события/счёта без ухода со страницы.

Бейджи вместо «стен текста» (аллергии/лекарства/справки).

Пустые состояния с CTA (например, «Нет документов — Загрузить»).

C. Локализация/форматы

Полный i18n (RU/KZ) для всех пользовательских строк.

Формат KZT (1 234 567 ₸) и Asia/Almaty для дат/времени.

Все журналы/логи — тоже в Asia/Almaty.

D. Доступность и качество

Контраст ≥ 4.5:1; фокус-контуры; aria для плеера/таблиц/чатов.

Skeleton-загрузки, friendly-ошибки («Плохая сеть — повторить»).

Горячие клавиши: / — фокус поиска, Ctrl/Cmd+K — глобальный поиск (опционально).

E. Перфоманс

Дэшборд LCP < 2.5 c (lazy для тяжёлых виджетов).

Плеер стартует < 2 c (показываем постер + спиннер).

WebSocket авто-reconnect + offline-очередь для чата (если сеть пропадёт).

Маршруты (для имплементации)

/dashboard — главная.

/children → /children/:id/(day|health|docs|billing)

/staff → /staff/:id

/billing/(invoices|subscriptions|services) → /billing/invoices/:invoiceId

/chat → /chat/:threadId

/settings

Мини-критерии приёмки (реплит)

На /dashboard ровно 4 карточки + мини-календарь; все CTA ведут в корректные разделы.

/children/:id/day показывает хронологию дня; health — структурированный профиль; docs — список с датами и статусами.

/billing/invoices — оплата ≤ 2 клика; services — нет влияния на «invoices».

/chat — отправка/приём сообщений в реальном времени, read receipts, вложения до X MB.

Локализация: переключение RU/KZ меняет текст и формат даты/валюты.

Таймзона: отметки в «Дне», чате, платежах — в Asia/Almaty.

Доступность: приложение проходимо с клавиатуры; aria-лейблы у ключевых контролов.

Безопасность: родитель не видит чужих детей/чатов/доков; разделы «health/docs» доступны только допущенным ролям.